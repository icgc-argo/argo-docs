import React from 'react';
import TableComp from '@icgc-argo/uikit/Table';
import { withTheme } from 'emotion-theming';

const Table = withTheme((props) => <TableComp {...props} />);

export default Table;
