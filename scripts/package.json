{
  "scripts": {
    "help": "node manage-versions.js -h --npm",
    "list": "node manage-versions.js -l --npm",
    "add": "node manage-versions.js -a --npm --silent",
    "debug-add": "node -nolazy --inspect-brk=9229 manage-versions.js -a",
    "clean": "node clean-data.js",
    "test": "mocha --inline-diff",
    "watch-tests": "mocha ./**/*.test.js --inline-diff -w"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.0.1"
  },
  "prettier": {
    "printWidth": 100,
    "trailingComma": "all",
    "singleQuote": true
  },
  "dependencies": {
    "axios": "^0.19.0",
    "chalk": "^3.0.0",
    "inquirer": "^7.0.0",
    "querystring": "^0.2.0",
    "yargs": "^15.0.1"
  }
}
